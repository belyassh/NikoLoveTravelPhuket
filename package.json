{
  "name": "niko-travel",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/generate-pages.cjs",
    "dev": "vite",
    "prebuild": "node scripts/generate-pages.cjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
